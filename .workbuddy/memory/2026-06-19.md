# 2026-06-19

## 项目创建
- 从 wails3_test_vue_ts 分叉，移除 demo 组件
- 重命名：changeme → PureRaw
- Go 模块 + Wails 绑定重新生成

## UI 骨架
- 三栏布局：左侧文件列表（w-64） + 中间预览区 + 底部评分工具栏
- 快捷键：1-5 评分，← → 导航
- 空状态提示：拖放文件夹或照片开始筛选
- 自定义标题栏（Frameless）

## 布局重构
- titlebar 从 fixed 改为普通 flex 子元素，去除 absolute/z-[9999]
- App.tsx 承担 w-screen/h-screen 和 titlebar + content 布局
- 所有路由在 app-content (flex-1) 内渲染
- 提交推送：20e0fed → main

## 文件加载功能实现
- 新建 `services/file_service.go`：SelectFolder（原生对话框）、GetFiles（递归扫描28种照片格式）、GetFileDataURI（Base64预览）
- Wails3 绑定生成器切换到 `.js` 输出，新增 `fileservice.js` + `models.js`（PhotoInfo）
- 前端 `stores/photos.ts`：Pinia store 管理照片数组/评分/导航/加载/预览
- `platform/wails.ts`：WailsFileAPI.selectFolder() 对接 FileService 绑定
- `Home.tsx` 重构：对接 store、文件拖放监听（Events.On）、图片预览渲染、Toolbar 按钮绑定
- 修复 `Button.vue` 缺少 onClick/disabled 类型 — shadcn-vue TSX 兼容
- 全部前后端编译通过，提交推送：b1a6ab9 → main
