{
  "name": "PureRaw",
  "version": "0.1.0",
  "description": "专业照片筛选工具 - AI 辅助 RAW 照片评分与筛选",
  "scripts": {
    "dev": "wails3 dev -config ./build/config.yml -port 9245",
    "build": "task build",
    "build:frontend": "cd frontend && npm run build",
    "package": "task package",
    "generate:bindings": "wails3 generate bindings -ts",
    "clean": "rimraf tmp bin frontend/dist frontend/bindings build-errors.log",
    "clean:all": "npm run clean && cd frontend && rimraf node_modules"
  },
  "author": "Heyana",
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^5.0.5",
    "task": "^1.3.2"
  }
}
